export default class SpawnpointTopRight {
    constructor (x, y, gridWidth, gridHeight) {
        this.x = x;
        this.y = y;
        const spawnpointTopRight = (x + 23, y + 2);
        this.gridWidth = gridWidth;
        this.gridHeight = gridHeight;
    }
}